name: PR

on:
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
    build:
        runs-on: [ ubuntu-20.04 ]
        steps:
        - name: Checkout
          uses: actions/checkout@v4
          with:
            fetch-depth: 0
